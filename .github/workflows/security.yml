name: Security Scan

on: [push]

jobs:
  scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Check scripts
        run: |
          echo "Scanning scripts..."
          ls -la
